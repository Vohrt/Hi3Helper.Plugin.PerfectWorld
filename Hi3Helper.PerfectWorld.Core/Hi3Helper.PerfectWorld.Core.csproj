<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <Platforms>x64</Platforms>
        <InvariantGlobalization>true</InvariantGlobalization>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <DebugType>embedded</DebugType>
        <Deterministic>true</Deterministic>
        <LangVersion>preview</LangVersion>
        <AssemblyName>PerfectWorld.Core</AssemblyName>
        <ProductName>Perfect World Core Plugin Library</ProductName>
        <Product>Collapse</Product>
        <Description>Common core functionality (Perfect World pw_sdk / PatcherSDK update mechanism) for Perfect World-published game plugins in Collapse Launcher</Description>
        <AssemblyTitle>Perfect World Core Library</AssemblyTitle>
        <Company>CollapsePlugin</Company>
        <Authors>$(Company).</Authors>
        <Copyright>Copyright 2026 $(Company)</Copyright>
        <Version>1.0.0</Version>
        <Configurations>Debug;Release;DebugNoReflection;ReleaseNoReflection</Configurations>
        <Title>Hi3Helper.PerfectWorld.Core</Title>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)'=='DebugNoReflection'">
        <DefineConstants>$(DefineConstants);DEBUG;USELIGHTWEIGHTJSONPARSER</DefineConstants>
        <Optimize>false</Optimize>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)'=='ReleaseNoReflection'">
        <DefineConstants>$(DefineConstants);USELIGHTWEIGHTJSONPARSER</DefineConstants>
        <Optimize>true</Optimize>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\Hi3Helper.Plugin.Core\Hi3Helper.Plugin.Core.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <!-- Managed HDiffPatch applier for block-level incremental updates (pw_sdk uncompressed HDIFF13 diffs).
             Referenced from the in-repo submodule fork (Vohrt/SharpHDiffPatch.Core) instead of the upstream
             NuGet package: the fork windows the in-memory output cache so patches whose target file exceeds
             2 GiB apply successfully. The stock 2.3.4/2.4.0 package throws at the ~2 GiB MemoryStream limit,
             which made every >2 GiB delta fall back to a full re-download. -->
        <ProjectReference Include="..\SharpHDiffPatch.Core\SharpHDiffPatch.Core\SharpHDiffPatch.Core.csproj"/>
    </ItemGroup>

    <ItemGroup>
        <!-- Prebuilt native helper that presses the vendor launcher's "开始游戏" button via DLL injection + Qt
             meta-object invocation (see Native\PwAutoClick\PwAutoClick.cpp and its build.ps1). It is committed as a
             binary (like Indexer.exe) and embedded so the plugin can extract and inject it at runtime with no extra
             files to ship. The Condition keeps the managed build working even if the DLL has not been compiled yet;
             LauncherAutoClickSession then simply reports the resource missing and falls back to the "/autoplay" path. -->
        <EmbeddedResource Include="Native\PwAutoClick.dll" Condition="Exists('Native\PwAutoClick.dll')">
            <LogicalName>PwAutoClick.dll</LogicalName>
        </EmbeddedResource>
    </ItemGroup>

</Project>
